import React from 'react';
import ReactDOM from 'react-dom/client';
import PC120Map from '../app/pc120-map';
import '../app/globals.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <PC120Map />
  </React.StrictMode>,
);
